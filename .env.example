# Copy this file to `.env` and replace the placeholders with real values.
# `.env` is git-ignored and must never be committed.

# Password used to connect to the student database.
DB_PASSWORD=your_db_password_here
